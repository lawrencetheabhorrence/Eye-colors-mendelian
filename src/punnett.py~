import random


def input_parameter(prompt: str, valid_values):


def input():
    print("-1 to exit")
    parent1 = input("Eye color of Parent 1 (Brown/Green/Blue): ")
    parent2 = input("Eye color of Parent 2 (Brown/Green/Blue): ")
    children = input("Number of c


def main():
    while(True):
        children = int(input("Number of children to generate (any nonpositive number/input to exit) : "))
        if not children > 0: break
        simulate(children)


main()
